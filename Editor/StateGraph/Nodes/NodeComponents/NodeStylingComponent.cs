using UnityEditor.Experimental.GraphView;
using UnityEngine;
using UnityEngine.UIElements;

namespace Nonatomic.VSM2.Editor.StateGraph.Nodes.NodeComponents
{
	public class NodeStylingComponent : INodeComponent
	{
		public void Initialize(BaseStateNodeView node)
		{
			// var styleSheet = Resources.Load<StyleSheet>(stylePath);
			// Assert.IsNotNull(styleSheet, $"{stylePath}.uss not found");
			// styleSheets.Add(styleSheet);
		}
	}
}